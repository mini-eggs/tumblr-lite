{
  "name": "index",
  "scripts": {
    "start": "parcel watch src/index.html --out-dir ../www --public-url /",
    "build": "parcel build src/index.html --out-dir ../www --public-url /"
  },
  "devDependencies": {
    "node-sass": "^4.7.2",
    "parcel-bundler": "^1.6.0",
    "postcss-modules": "^1.1.0"
  },
  "dependencies": {
    "preact": "^8.2.7",
    "preact-router": "^2.6.0",
    "unistore": "^3.0.4"
  }
}
